<Project>

  <!-- NuGet -->
  <PropertyGroup Condition="'$(GeneratePackageOnBuild)' == 'true'">
	<!-- Source: https://github.com/dotnet/sourcelink/#using-sourcelink -->
	<!-- Optional: Publish the repository URL in the built .nupkg (in the NuSpec <Repository> element) -->
	<PublishRepositoryUrl>true</PublishRepositoryUrl>
	<!-- Optional: Embed source files that are not tracked by the source control manager in the PDB -->
	<EmbedUntrackedSources>true</EmbedUntrackedSources>
	<!-- Optional: Include the PDB in the built .nupkg -->
	<AllowedOutputExtensionsInPackageBuildOutputFolder>$(AllowedOutputExtensionsInPackageBuildOutputFolder);.pdb</AllowedOutputExtensionsInPackageBuildOutputFolder>
  </PropertyGroup>
  <ItemGroup Condition="'$(GeneratePackageOnBuild)' == 'true'">
	<PackageReference Include="Microsoft.SourceLink.GitHub" Version="1.0.0-beta2-18618-05" PrivateAssets="All" />
  </ItemGroup>
  <!-- /NuGet -->
  
  <!-- Publish NuGet -->
  <PropertyGroup Condition="'$(GeneratePackageOnBuild)' == 'true'">
	<PackageOutputPath Condition="'$(PublishDirectory)' != ''">$(PublishDirectory)\NuGet</PackageOutputPath>
  </PropertyGroup>
  <!-- /Publish NuGet -->

</Project>